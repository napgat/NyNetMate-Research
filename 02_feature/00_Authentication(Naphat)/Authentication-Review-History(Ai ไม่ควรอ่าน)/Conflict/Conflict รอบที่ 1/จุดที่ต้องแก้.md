ตรวจรอบสองแล้ว เอกสารดีขึ้นชัดเจนและแก้ประเด็นเดิมส่วนใหญ่ได้จริง แต่ยังเป็นสถานะ “Approve with revisions” — พร้อมใช้เป็นแบบร่างสถาปัตยกรรม แต่ยังไม่ควรส่งให้ทีม implement ทันที

สรุปการตรวจคำกล่าวอ้าง:

|หัวข้อ|ผลตรวจ|
|---|---|
|Schema reconciliation|แก้ใน Auth แล้ว แต่ Source of Truth กลางยังขัดกัน|
|Audit contract|ดีขึ้น แต่ Central Audit Schema ยังไม่มีฟิลด์รองรับ|
|ยกเลิก 423|ผ่าน|
|Temporary password|มี schema/flow แล้ว แต่ backend guard ยังไม่ชัด|
|Change/reset password|ผ่านบางส่วน ยังต้องแยก session behavior|
|CORS/CSRF|API แก้ถูกขึ้น แต่ Glossary ยังกล่าวเกินจริง|
|API พร้อม implement|ยังไม่ครบ|
|Permission catalog|สร้างแล้ว แต่มีสิทธิ์ดู Credential Secret ที่ควรแก้|
|Reference files|เปลี่ยนชื่อแล้ว แต่ควรติดป้าย Non-normative ในเนื้อหา|
|Acceptance tests|ดีขึ้น แต่ยังขาด test-user specification และรายละเอียดบางกรณี|


## จุดเอกสารที่ควรเก็บให้เรียบร้อย

- [01_MVP (line 52)](/E:/CEPP Project/หลักศูตร/KMITL_Knowledge/Project/02_feature/00_Authentication(Naphat\)/01_MVP - Authentication & RBAC.md:52) ยังมีประโยค “ทำไมต้องมี 3 Roles 2 Role ไม่ได้หรอ” หลุดกลางเอกสาร
- Logout บรรทัด 60 บอกว่า “ลบ session ID” แต่ schema ใช้ `is_revoked=true`; ควรเลือก revoke เพื่อรักษาประวัติ
- [Glossary (line 10)](/E:/CEPP Project/หลักศูตร/KMITL_Knowledge/Project/02_feature/00_Authentication(Naphat\)/00_Glossary.md:10) ยังบอกว่า `SameSite=Lax` “ป้องกัน CSRF” ควรเปลี่ยนเป็น “ช่วยลดความเสี่ยง”
- Acceptance Test ของ Origin ลืม `PATCH` ทั้งที่ API Contract รวมไว้
- Permission Catalog ควรมีคอลัมน์ `Phase` เพื่อทำเครื่องหมาย `topology.*` และ `deploy.execute` เป็น P2
- `REF_GPT-MVP.md` ควรมีข้อความบนสุดว่า “Historical reference — non-normative, ห้ามใช้เป็น implementation contract” เพราะเนื้อหาภายในยังมีข้อมูลเก่า
- `REF_คำถาม.md` ดีขึ้น แต่คำว่า “มาตรฐานความปลอดภัยระดับองค์กร” ควรมีแหล่งอ้างอิงหรือเปลี่ยนเป็น “หลักการด้านความปลอดภัยที่ใช้ทั่วไป”

หลังปิด 9 จุด P1 ด้านบน เอกสารจะพร้อมส่งให้ทีม Backend/Frontend แตก implementation task ได้ครับ