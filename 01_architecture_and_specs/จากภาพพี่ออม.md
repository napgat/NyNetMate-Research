## 🧠 สรุปว่า Mockup นี้บอกอะไรบ้าง

Mockup นี้มี **6 Tab** ซึ่งแต่ละ Tab = **1 Config Use Case** ที่ระบบต้องรองรับ:

---

### 📋 Tab 1 — Device (Basic Identity + Management)

|Section|Field|ประเภทข้อมูล|⚠️ PII?|
|---|---|---|---|
|Basic Identity|Hostname|Text|❌|
|Basic Identity|Domain name|Text|❌|
|Basic Identity|Vendor|Dropdown (Cisco IOS/NX-OS, Juniper, MikroTik, Arista)|❌|
|Basic Identity|Device role|Dropdown (Core switch, Router ฯลฯ)|❌|
|Basic Identity|Site/location|Text|❌|
|Management|Management IP|IP Address|✅ ต้อง Mask|
|Management|Subnet mask|Text|✅ ต้อง Mask|
|Management|Default gateway|IP Address|✅ ต้อง Mask|
|Management|Enable password|Password|✅ **Critical**|
|Management|SSH username|Text|✅ **Critical**|
|NTP & DNS|NTP server 1, 2|IP/Hostname|⚠️ บางกรณี|
|NTP & DNS|DNS server 1, 2|IP Address|⚠️ บางกรณี|
|SNMP|Community RO/RW|String|✅ **Critical**|
|SNMP|SNMP trap host|IP Address|✅ ต้อง Mask|

---

### 🔌 Tab 2 — Interfaces

|Field|ค่าตัวอย่าง|หมายเหตุ|
|---|---|---|
|Name|GigabitEthernet0/0|Interface identifier|
|IP address|192.168.1.1/24|L3 IP (ถ้ามี)|
|Description|Uplink|Human-readable label|
|Mode|Access / Trunk / Routed / Loopback|L2 vs L3|
|Status|Up / Shutdown|Admin state|

---

### 🗃️ Tab 3 — VLANs

|Field|ค่าตัวอย่าง|หมายเหตุ|
|---|---|---|
|VLAN ID|10, 20, 30|1-4094|
|Name|MGMT, DATA, VOICE|Label|
|SVI IP|10.0.0.1/24|Layer 3 Gateway (Optional)|

---

### 🗺️ Tab 4 — Routing

**Static Routes:**

- Destination, Subnet mask, Next-hop

**Dynamic Routing:**

- Protocol: OSPF / BGP / EIGRP / RIP / None
- Process ID / AS number
- Router ID
- Networks (one per line)

---

### ⚙️ Tab 5 — Services (Toggle Checkboxes)

✅ SSH v2              ❌ Telnet (ปิดแนะนำ)

❌ HTTP server         ✅ HTTPS server

✅ CDP                 ✅ LLDP

✅ Spanning Tree (STP) ❌ PoE

**Banner & Logging:**

- Login banner (MOTD)
- Syslog server IP
- Log level (Informational / Warning / Error)

---

### 👁️ Tab 6 — Config Preview

> _"generates real vendor-specific syntax as you type"_

= นี่คือ **Jinja2 Template Layer** ที่ render ออกมาเป็น Config จริงแบบ Real-time + ปุ่ม **"Ask Claude to review"** = Security Validation (CIS Benchmarks)
## 🔍 วิเคราะห์ Config Preview Tab

### Config ที่ Generate ออกมาจริงๆ:

```cisco

! Generated by NetAutoConfig    ← ชื่อระบบที่เพื่อนตั้งชั่วคราว

! Vendor: Cisco IOS

!

hostname DEVICE

service password-encryption     ← CIS Rule อัตโนมัติ

!

crypto key generate rsa modulus 2048   ← CIS Rule อัตโนมัติ

ip ssh version 2                ← CIS Rule อัตโนมัติ

no service telnet               ← CIS Rule อัตโนมัติ

no ip http server               ← CIS Rule อัตโนมัติ

ip http secure-server           ← CIS Rule อัตโนมัติ

lldp run

!

vlan 10

 name MGMT

!

interface Vlan10                ← SVI ที่ Generate จาก VLAN Tab

 description SVI-MGMT

 ip address 10.0.0.1 255.255.255.0

 no shutdown

!

vlan 20

 name DATA

!

vlan 30

 name VOICE

!

interface GigabitEthernet0/0   ← Interface ที่ Generate จาก Interface Tab

 description Uplink

 switchport mode trunk

 switchport trunk encapsulation dot1q

 no shutdown

!

end
```
---

## 💡 สิ่งที่ Tab นี้เฉลยให้

### 1. ✅ ยืนยันว่า Template-first คือทางถูก

Config ที่ออกมา **ไม่ได้ใช้ AI เลยแม้แต่นิดเดียว** — มันมาจาก **Jinja2 Template ล้วนๆ** ที่รับข้อมูลจาก Form แล้ว Render ออกมา ตรงกับที่อาจารย์บอกว่า:

> _"Rule-based/Template เป็นแกนหลัก AI เป็นแค่ผู้ช่วย"_

### 2. ✅ CIS Benchmarks บางข้อ ระบบใส่ให้อัตโนมัติเลย

สังเกตว่า Services Tab มีแค่ Checkbox แต่ Config ออกมาเป็น:

|Checkbox ที่กด|Config ที่ Generate|
|---|---|
|✅ SSH v2|`crypto key generate rsa modulus 2048` + `ip ssh version 2`|
|❌ Telnet|`no service telnet`|
|❌ HTTP|`no ip http server`|
|✅ HTTPS|`ip http secure-server`|
|✅ LLDP|`lldp run`|

= นี่คือ **CIS Benchmark Rule-based** ที่ทำได้โดยไม่ต้องใช้ AI เลย! เขียน Template ครั้งเดียวจบ

### 3. ✅ Jinja2 Template Structure ที่ต้องเขียน

เห็นชัดเลยว่า Template จะหน้าตาประมาณนี้:

```
```jinja2

! Generated by SkaiNet

! Vendor: {{ device.vendor }}

!

hostname {{ device.hostname }}

service password-encryption

!

{% if services.ssh_v2 %}

crypto key generate rsa modulus 2048

ip ssh version 2

{% endif %}

{% if not services.telnet %}

no service telnet

{% endif %}

{% if not services.http %}

no ip http server

{% endif %}

{% if services.https %}

ip http secure-server

{% endif %}

{% if services.lldp %}

lldp run

{% endif %}

!

{% for vlan in vlans %}

vlan {{ vlan.id }}

 name {{ vlan.name }}

{% if vlan.svi_ip %}

!

interface Vlan{{ vlan.id }}

 description SVI-{{ vlan.name }}

 ip address {{ vlan.svi_ip }}

 no shutdown

{% endif %}

!

{% endfor %}

{% for iface in interfaces %}

interface {{ iface.name }}

 description {{ iface.description }}

{% if iface.mode == 'trunk' %}

 switchport mode trunk

 switchport trunk encapsulation dot1q

{% elif iface.mode == 'access' %}

 switchport mode access

{% endif %}

 no shutdown

!

{% endfor %}

end
```

### 4. ✅ "Ask Claude to review" = จุดที่ AI เข้ามา

ปุ่มนี้คือ **AI Layer** ที่อาจารย์พูดถึง:

Form → Jinja2 Template → Config Preview

                                ↓

                    [Copy config] → ใช้งานเลย

                    [Ask AI to review] → PII Mask → Gemini → Security Feedback

---

## 📦 สรุปภาพรวมทั้ง 6 Tab สำหรับ Day 1


> **สรุปคือ Mockup นี้ = MVP ของระบบเราทั้งหมดอยู่ใน 6 Tab** ถ้าเพื่อนทำ Frontend แบบนี้ได้แล้ว งานที่เหลือคือ Backend (FastAPI + Jinja2 Template + PostgreSQL) และ AI Review Layer (Presidio + Gemini) ครับ 🎯